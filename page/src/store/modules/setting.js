
const setting = {
  state: {
    // api_url:'/',
    api_url:'?r=',
    pagination: {
      limit: 30,
      tableMax: 590,
      layout: 'total, prev, pager, next'
    }
  }
}

export default setting
